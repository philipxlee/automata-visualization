package cellsociety.model.celltypes;

import cellsociety.model.Cell;

public class SchellingCell extends Cell {
  public SchellingCell(int row, int col, String state) {
    super(row, col, state);
  }
}
