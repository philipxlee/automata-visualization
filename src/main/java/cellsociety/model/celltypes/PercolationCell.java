package cellsociety.model.celltypes;

import cellsociety.model.Cell;

public class PercolationCell extends Cell {
  
  public PercolationCell(int row, int col, String state) {
    super(row, col, state);
  }
}
